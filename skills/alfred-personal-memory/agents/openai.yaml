interface:
  display_name: "Alfred Memory"
  short_description: "Recall and update Alfred's local memory"
  default_prompt: "Use $alfred-personal-memory to recall relevant context and save lasting personal knowledge."

policy:
  allow_implicit_invocation: true
